# Docker Compose for local development
# Provides Redis for background job processing

services:
  redis:
    image: redis:7-alpine
    container_name: directorai-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  redis_data:
